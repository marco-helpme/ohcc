export default function ({ store, redirect }) {
  let user = {}
  user = store.users
  console.log(user)
  // if (user.users.user === null) {
  //   redirect('/')
  // }
  // if (user.users.user.id_rol !== '3') {
  //   redirect('/')
  // }
}
